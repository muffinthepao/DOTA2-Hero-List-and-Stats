// let picks = [];
            // let wins = [];
            // Object.keys(hero).forEach((key) => {
            //     if (key.indexOf("_pick") > -1) {
            //         picks.push(key);
            //     } else if (key.indexOf("_win") > -1) {
            //         wins.push(key);
            //     }
            // });

            // picks = picks.map((key) => hero[key]);
            // wins = wins.map((key) => hero[key]);

            // const picks = [
            //     hero['1_pick'],
            //     hero['2_pick'],
            //     hero['3_pick'],
            //     hero['4_pick'],
            //     hero['5_pick'],
            //     hero['6_pick'],
            // ]